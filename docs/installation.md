# Instalación

1. Clonar el repositorio:
   git clone https://github.com/cunef-isabella-jaime/titanic-prediction-service

2. Crear entorno virtual:
   python -m venv venv

3. Activarlo:
   venv\Scripts\activate

4. Instalar dependencias:
   pip install -r requirements.txt
